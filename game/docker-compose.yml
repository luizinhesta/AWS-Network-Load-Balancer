# Docker Compose para orquestração local do servidor Luanti
# Porta UDP/TCP 30000 para conexões de jogadores e health check
# Volume local para persistência dos dados do mundo

services:
  luanti-server:
    build: .
    container_name: luanti-server
    ports:
      - "30000:30000/udp"
      - "30000:30000/tcp"
    volumes:
      - world-data:/config/worlds
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Sao_Paulo
      - CLI_ARGS=--gameid devtest
    restart: unless-stopped

volumes:
  world-data:
    driver: local
